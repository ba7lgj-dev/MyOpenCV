# 自动加气串口测试说明

本项目的自动加气控制基于 CP2102 串口模块，使用 **低电平触发、高电平关闭** 方式驱动继电器/电磁阀。

## 串口测试入口
- 在应用菜单栏直接点击 **“摄像头管理”** 或 **“自动加气配置”**（已展开，不再折叠在“系统设置”内）。
- 打开“自动加气配置”对话框后，选择要测试的串口和脉冲时长，点击 **“测试串口”** 按钮即可。

## 工作机制
1. 对话框会按当前选择的串口名创建 CP2102 控制器，并将 DTR/RTS 拉高，确保初始状态为关闭。
2. 点击“测试串口”后会立即发出一次低电平脉冲（时长由“单次加气时长”设置，范围 50–20000ms）。
3. 脉冲结束后自动恢复为高电平，关闭加气。
4. 结果会显示在对话框底部状态标签，同时写入应用日志，便于排查串口可用性。

## 额外提示
- 若测试失败，请检查串口占用情况或权限，确保选择的端口名正确。
- 冷却时间等安全参数仍遵循配置文件中的限制，不影响单次测试行为。
